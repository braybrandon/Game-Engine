namespace GameEngine.Common.Interfaces
{
    /// <summary>
    /// Represents the interface for an event system, which manages the dispatching and handling of events within the engine.
    /// Implementations should provide mechanisms for subscribing, publishing, and processing events.
    /// </summary>
    public interface IEventSystem
    {
    }
}
